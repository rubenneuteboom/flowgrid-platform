# =============================================================================
# Flowgrid Platform - Azure Container Apps nginx Configuration
# Uses full internal FQDNs for Container Apps service discovery
# =============================================================================

server {
    listen 80;
    server_name _;
    
    # Use Azure internal DNS resolver
    resolver 168.63.129.16 valid=30s ipv6=off;
    
    # Relative redirects
    absolute_redirect off;
    
    # Increase timeouts for Azure
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Root for static files
    root /usr/share/nginx/html;
    index index.html;
    
    # Health check endpoint (must be first)
    location = /health {
        access_log off;
        return 200 'OK';
        add_header Content-Type text/plain;
    }

    # API routes with full internal FQDNs
    # Format: <app-name>.internal.<env-default-domain>
    location /api/auth {
        set $upstream_auth auth-service.internal.salmonfield-b588dc13.westeurope.azurecontainerapps.io;
        proxy_pass http://$upstream_auth;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /api/agents {
        set $upstream_agent agent-service.internal.salmonfield-b588dc13.westeurope.azurecontainerapps.io;
        proxy_pass http://$upstream_agent;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /api/relationships {
        set $upstream_agent agent-service.internal.salmonfield-b588dc13.westeurope.azurecontainerapps.io;
        proxy_pass http://$upstream_agent;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /api/wizard {
        set $upstream_wizard wizard-service.internal.salmonfield-b588dc13.westeurope.azurecontainerapps.io;
        proxy_pass http://$upstream_wizard;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /api/integrations {
        set $upstream_integration integration-service.internal.salmonfield-b588dc13.westeurope.azurecontainerapps.io;
        proxy_pass http://$upstream_integration;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /design {
        set $upstream_design design-module.internal.salmonfield-b588dc13.westeurope.azurecontainerapps.io;
        proxy_pass http://$upstream_design;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /shell {
        set $upstream_design design-module.internal.salmonfield-b588dc13.westeurope.azurecontainerapps.io;
        proxy_pass http://$upstream_design;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Default: serve static files or 404
    location / {
        try_files $uri $uri/ /index.html =404;
    }
}
